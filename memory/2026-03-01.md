# 2026-03-01

## What changed
- Implemented a Hub-level i18n system in `light-math-hub` with `zh-CN` and `en-US`.
- Added language context/provider and centralized UI copy catalog.
- Added app metadata localization support via `apps.json` `i18n.en-US`.
- Wired locale-aware rendering for top nav, list, detail, launch, pagination, and state pages.

## Key decisions
- Default locale is `zh-CN`.
- User selection is persisted in `localStorage` key `light-math-hub.locale`.
- Kept existing app packaging and hub registry contracts unchanged; localization is additive.

## Open issues
- No functional blockers found during build checks.

## Next actions
- Optional: extend localization to other modules (`light-maths` app UIs) if full bilingual experience is required end-to-end.

## Update (algebra-balance drop fix)
- Improved drag-drop success rate in `light-maths/apps/algebra-balance`.
- Expanded pan drop detection with tolerant hit padding in `App.tsx`.
- Enlarged left/right pan ref containers in `components/Beam.tsx` so drop targets are physically larger.
- Verified with `cd light-maths/apps/algebra-balance && npm run build` (passed).
- Algebra-balance visual update: negatives now hang from each pan rod top (upward pull), positives remain on pans; drag behavior kept.
- Algebra-balance positioning fix: left/right pan assemblies now use explicit inline left/right positioning near beam ends to avoid center overlap; hanging negatives stay attached to each side rod.
- Reverted algebra-balance to pre-hanging-balloon adjustment state at user request (Beam/Pan layout before rod-top hanging visuals).
- Adjusted only hanging behavior: negatives render at each rod top in `Beam`, positives remain on pans in `Pan`; no beam/rod position constants changed.
- Increased negative-balloon hanging hit area with hidden rectangular zones (left/right) and wired drop detection to prioritize these zones for negative terms.
- Removed visible trash drop zone; both negatives and positives now add only through hidden sensing rectangles, and dragging an existing item out of sensing zones removes it.
- Added same-style cancellation effect on each pan side: cancellable pairs (x with -x, 1 with -1) are visually faded for both positive and negative items.
- Added manual problem editor in algebra-balance: users can edit left/right coefficients and x value, with integer validation and equation-consistency check before applying.
- Integrated `algebra-balance` into Hub registry and local permission list.
- Standardized `algebra-balance` packaging contract (`light-app.json`, `game-spotlight.html`, Vite base/outDir multi-entry) so `build:all` and deploy script include it.
- Deployed to production via `scripts/deploy-math-chat1.sh`; release `/var/www/math.chat1.co/releases/20260301-203401`; HTTP 200 verified for `/` and `/apps/algebra-balance/`.
- Updated `algebra-balance` app title/root typography to the unified Chinese UI font stack (`PingFang SC`, `Microsoft YaHei`, `Helvetica Neue`, `Roboto`, `Droid Sans Fallback`, `WenQuanYi Micro Hei`).
- Removed remaining decorative webfont usage in Hub / `cube-surface-lab` / `fraction-lab` titles; all app title text now uses the same system Chinese font stack to avoid runtime font swap.
- Audited all apps under `light-maths/apps` and aligned `density` to the same Chinese system font stack; removed `font-sans` utility reliance from app roots where needed to prevent future title font drift.
- Added missing `light-maths/apps/algebra-balance/index.css` with the same Chinese font stack so the app no longer emits missing stylesheet warnings during build and keeps typography consistent at document level.

## Update (hub icon presentation)
- Simplified Hub list-card icon rendering in `light-math-hub/src/components/AppCardGrid.tsx` to a single icon container (removed nested media + inner badge layering).
- Enlarged list-card icon and detail-page icon in `light-math-hub/src/styles/index.css` for better readability.
- Updated icon image fit mode to `contain` so image icons stay完整 and no longer appear cramped.
- Verified with `cd light-math-hub && npm run build` (passed).
- Verified with `cd light-maths && npm run build:all` (passed).

## Update (hub icon app-store style)
- Removed remaining outer visual frame around list icons by making `.app-card__media` layout-only (no border/background/padding).
- Updated list icon style to single rounded-square block (`100x100`, larger corner radius and shadow) to match App Store-like single-layer icon presentation.
- Set image icon fill to `object-fit: cover` so image assets fill the icon block cleanly.
- Verified with `cd light-math-hub && npm run build` (passed).

## Update (cross-project note)
- Wrote reusable icon refactor note for PlayHub at `/Users/lee/git2/playhub/docs/app-icon-single-layer-note.md`.
- Document includes JSX structure simplification, CSS parameters, mobile suggestions, and acceptance checklist for App Store-like single-layer icons.
