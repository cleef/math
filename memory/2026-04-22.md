# 2026-04-22

## 任务记录（Codex）
- 产出：按 `/Users/lee/git/zm-light/zm-light-app-hub` 首页作为参考，重做 `light-math-hub` 首页视觉与卡片交互。
- 代码变更：
  - `light-math-hub/src/styles/index.css` 切换为 `zm-light` 首页风格基线，并补充当前项目需要的 `top-nav__lang`、`tag-filter`、`spotlight-frame` 等样式。
  - `light-math-hub/src/components/AppCardGrid.tsx` 改为卡片空白区点击直接打开 `/run/:id`（新标签页），hover/focus 显示“介绍 + 打开”操作区，详情路由保持 `/app/:id`。
  - `light-math-hub/src/components/TopNav.tsx` 补充 `brand-copy` 结构类名以对齐导航布局。
  - `light-math-hub/src/i18n/messages.ts` 新增 `appCard.openButton` 中英文文案。
- 验证：`cd light-math-hub && npm run build` 通过。
- 未决问题：工作区存在与本次任务无关的预置变更 `docs/exam_revision/math_202604x.md`（删除状态），未处理。
- 下一步：建议本地启动 Hub 人工确认桌面端/移动端首页交互（卡片 hover 与触屏回退表现）。

## 任务记录（Codex）
- 产出：为 Hub 首页补充“呼吸感”动效节奏（氛围层 + 入场层 + 微交互层）。
- 代码变更：
  - `light-math-hub/src/styles/index.css`：新增背景光晕慢漂移（`auraDrift/auraPulse`）、导航柔和呼吸与扫光（`navBreath/navSheen`）、筛选面板微呼吸（`panelBreath`）、卡片错峰入场（`cardRise`）与光晕呼吸（`cardAura`）；并在 `prefers-reduced-motion` 下显式降级。
  - `light-math-hub/src/components/AppCardGrid.tsx`：为卡片注入 `--card-index`，用于错峰动画延迟。
- 验证：`cd light-math-hub && npm run build` 通过。
- 未决问题：暂无。
- 下一步：如需更强“呼吸感”，可追加滚动触发的分段 reveal（首屏外卡片进视口再入场）。

## 任务记录（Codex）
- 产出：前端首页隐藏 tag 筛选，列表仅保留关键词搜索过滤。
- 代码变更：`light-math-hub/src/routes/AppListPage.tsx` 删除 tag 筛选 UI 与 tag 过滤逻辑；同时在页面参数规范化中自动清理旧 `tag` 查询参数。
- 验证：`cd light-math-hub && npm run build` 通过。
- 未决问题：暂无。
