# 2026-04-24

- Reworked `docs/learning-platform-arch.md` from a generic ChatGPT draft into an execution-oriented migration plan based on the current two-project structure (`light-math-hub` + `light-maths`).
- Updated decision: implement the final `learning` structure directly now that GitHub is `https://github.com/cleef/learning` and `learning.chat1.co` DNS is set.
- Target renames: `/Users/lee/git/math` -> `/Users/lee/git/learning`, `light-math-hub` -> `light-learning-hub`, and `light-maths` -> `light-learning-apps`.
- Proposed first English app path: `light-learning-apps/apps/english-phonics-lab/`, reusing the existing `/apps/<id>/` URL contract and `docs/phonics-system.md`.
- Homepage direction refined: `learning.chat1.co` should be a lightweight subject entrance, closer to ChatGPT-style navigation, with Math/English first and app lists moved into subject pages.
- Public subject routes should be short paths (`/math`, `/english`) rather than `/subject/math`; `subject` remains an internal registry field.
- Plan review follow-up: subject routes must use an explicit whitelist and fixed routes (`/app`, `/run`, `/apps`) must match before `/:subject`.
- Plan review follow-up: production `current` should be managed only as a deployment symlink; pre-create `releases`, not `current`, and keep the existing current-directory backup guard.
- Inspected production `root@47.116.122.50`: `math.chat1.co` uses `/etc/nginx/sites-available/math.chat1.co.conf`, symlinked into `sites-enabled`, with an independent Certbot certificate under `/etc/letsencrypt/live/math.chat1.co/`; `certbot-renew.timer` is active.
- Expanded the learning migration plan with a production HTTPS certificate setup/update procedure for `learning.chat1.co`, following the existing `math.chat1.co` pattern.
- Implemented final learning structure inside the current repo root: renamed `light-math-hub` to `light-learning-hub`, `light-maths` to `light-learning-apps`, and `deploy-math-chat1.sh` to `deploy-learning-chat1.sh`; root directory remains `/Users/lee/git/math` for the user to rename manually.
- Implemented Learning Shell routes: `/` is a subject entrance, `/math` and `/english` are whitelisted subject pages, and app routes `/app/:id`, `/run/:id`, `/apps/<id>/` remain compatible.
- Added `english-phonics-lab`, registered it in Hub with `subject: "english"`, and added local access via `light.subject.english` / `light.app.english.phonics.lab`.
- Updated local git remote to `https://github.com/cleef/learning.git`.
- Configured production `learning.chat1.co` on `root@47.116.122.50` with independent Nginx site and Certbot certificate; deployed release `/var/www/learning.chat1.co/releases/20260424-224746`; HTTPS smoke checks for `/`, `/math`, `/english`, app detail/run shell paths, and `/apps/english-phonics-lab/` passed.
- Verification passed: `cd light-learning-hub && npm run build`, `cd light-learning-apps/apps/english-phonics-lab && npm run build`, `cd light-learning-apps && npm run build:all`, `cd light-learning-apps && npm run list:apps`, Playwright snapshots for home/English/app page, and `certbot renew --dry-run --cert-name learning.chat1.co`.
