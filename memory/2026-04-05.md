## allocation-expression-lab
- 迭代：将演示后的“选符号 / 方程测试”区域改为“Case 1 / Case 2”关系表达卡，不再要求学生先做方程判断。
- 决策：新的表达方式以三段关系呈现，按场景展示“房间/床位/人数”或“袋子/袋里苹果/苹果总数”，并保留 `x` 滑杆观察数值变化。
- 迭代：播放区标题文案同步从“条件 1 / 条件 2”改为 `Case 1 / Case 2`。
- 迭代：关系卡已进一步改为真实情景表达，不再引入 `x` 或滑杆；例如房间场景直接显示 `房间 6 -> 床位 6 × 10 -> 人数 6 × 10 - 12`。
- 验证：`cd light-maths/apps/allocation-expression-lab && npm run build` 通过。
- 下一步：如需继续细化，可进一步压缩关系卡上的辅助文字，只保留最核心的表达链。

## repo docs
- 变更：新增根目录 `SOUL.md`，作为项目气质与教学哲学文档。
- 决策：文档采用 Caucher Birkar 对数学的表述作为核心灵感，将数学定义为“ideas 的世界”，强调创造性、乐趣、低压、超越教材、play-based 学习，以及 AI 作为辅助而非灵魂。
- 影响：后续 app 设计可据此优先判断是否真正帮助学生把数学感受到为可探索、可创造、可提问的东西。
- 变更：更新根目录 `AGENTS.md`，在高价值路径中加入 `SOUL.md`，并要求 session start 先读 `SOUL.md`，再读 `MEMORY.md` 与当日/前一日记忆。
- 迭代：进一步将 `AGENTS.md` 改成接近 openclaw 的 `Every Session` 结构，明确顺序为 `SOUL.md` -> 今日与昨日 memory -> 主会话时再读 `MEMORY.md`，不引入 `USER.md`。
