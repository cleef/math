# 2026-02-23

## Changes made
- Created `light-math-hub` (React + Vite) based on PlayHub architecture and converted UI/content to math teaching domain.
- Created `light-maths` with app host scripts, API gateway, app template, and sample app `fraction-lab`.
- Added root deployment script: `scripts/deploy-math-chat1.sh` with default production domain `math.chat1.co`.
- Added/updated docs:
  - `README.md`
  - `docs/2026-02-23-math-chat1-deploy.md`
  - `light-math-hub/README.md`
  - `light-maths/README.md`
  - `light-maths/docs/light-app-standard.md`
- Updated `AGENTS.md` to reflect new multi-project architecture.

## Key decisions
- Preserve `/apps/<id>/` contract for hub-to-app routing and deployment layout.
- Keep `game-spotlight.html` as build entry for compatibility, while hub now also probes `lesson-spotlight.html` first.
- Use `fraction-lab` as initial production app and register permission `light.app.fraction.lab`.

## Verification
- `cd light-math-hub && npm run build` passed.
- `cd light-maths && npm run build:all` passed.
- `./scripts/deploy-math-chat1.sh --dry-run --skip-build` passed.

## Open issues
- Hub dependencies currently report 2 moderate vulnerabilities from `npm audit` (not fixed in this pass).

## Next actions
- Optionally add more math apps under `light-maths/apps/` and register them in hub.
- Run a real deployment and verify nginx/current symlink on remote host.

## Deployment session update (math.chat1.co)
- Verified remote nginx had no `math.chat1.co` vhost and no `/var/www/math.chat1.co` deployment path.
- Created and enabled nginx site for `math.chat1.co` and issued TLS via Certbot.
- Reissued `chat1.co` cert to remove `stock.chat1.co` domain from SAN list.
- Moved `stock.chat1.co` symlink out of `sites-enabled` so it is no longer loaded by nginx.
- Deployed release `20260223-144514` and fixed `current` symlink (initially a directory, caused `/apps/fraction-lab/` 500).
- Added deploy script hardening to auto-handle `current` directory vs symlink conflict.
- Final checks: `http://math.chat1.co` -> 301 HTTPS, `https://math.chat1.co/` 200, app paths 200.

## New app session update (cube-surface-lab)
- Added new app folder: `light-maths/apps/cube-surface-lab`.
- Implemented page 1 for cube surface learning:
  - edge selection on a 12-edge cube model
  - scissor simulation animation for each selected cut
  - unfoldability judgement after cuts
  - success-only unfold verification animation
- Added spotlight page content for the new app.
- Updated app metadata/package fields to `cube-surface-lab`.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed (cube-surface-lab + fraction-lab).

### Key decisions
- Use a tree-based simplified unfold rule: remaining uncut face graph must be connected, acyclic, and have 5 edges.
- Keep implementation fully local to `light-maths/apps/cube-surface-lab` without cross-project refactor.

### Open issues
- New app is not yet registered in hub `light-math-hub/src/data/apps.json` (intentionally out of this task scope).

### Next actions
- If needed, register the app in hub and grant corresponding dev permission in `currentUser.ts`.

## Update: 11 nets demo page
- Added a second in-app page for `cube-surface-lab`: `第2页：11种展开演示`.
- Implemented all 11 cube nets as selectable demos.
- For each net, added:
  - cut-edge list (7 cut edges)
  - unfold animation (folded -> target net)
  - final unfolded shape panel
- Added internal net mapping logic:
  - maps each net layout cell to cube face via orientation folding
  - derives hinge edges and cut edges from face adjacency

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: door opening direction
- In page-2 cube door interaction, `Door Angle` positive direction now opens outward from cube (not inward).
- Adjusted hinge rotation sign mapping in `getDoorFaceStyle`.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.

## Update: door interaction on original cube stage
- Moved door opening visualization onto the same top cube stage used for cutting interaction.
- Removed separate lower mini-cube presentation from the page-2 flow.
- Implemented in-place door face rotation directly on the SVG cube face polygon around hinge-side midpoint.
- When door opens, non-hinge edges of that door face are hidden in edge overlay to avoid visual ghost lines.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: hinge-fix for door opening
- Reworked door transform to keep hinge edge stable instead of rotating full face around center.
- New face update logic:
  - hinge-edge vertices remain fixed
  - opposite-edge vertices move using hinge-axis projection + angle shrink + outward lift
- This removes the artifact where uncut hinge structure appeared to move with the opening face.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: per-face door angle persistence
- Replaced single `Door Face` selector with per-face angle controls.
- Added a slider row for each face (F/B/U/D/L/R); angle state is preserved per face.
- Face sliders are enabled only when that face is openable under current cut state.
- Cube rendering now supports multiple opened faces simultaneously and keeps z-order readable.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: view rotation and larger drawing area
- Added viewport controls for page-2 cube stage:
  - `Yaw` slider
  - `Pitch` slider
  - `Reset View` action
- Applied view transform on cube stage wrapper so users can inspect current state from multiple angles.
- Enlarged page-2 drawing area and expanded SVG viewbox margins so opened faces are easier to observe.
- Added responsive fallback for controls on small screens.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: free-rotation feel
- Replaced slider-based view controls with drag-to-orbit interaction on page-2 cube canvas.
- New behavior:
  - pointer drag rotates view continuously
  - yaw can rotate freely for 360-degree feel
  - pitch is clamped to keep model readable
  - added click suppression after drag to avoid accidental edge cuts
- Kept `Reset View` button.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: face-letter reference normalization
- Adjusted face-label mapping root rule for nets:
  - choose a front reference cell by neighbor degree priority + center proximity
  - use that cell as `F` root orientation seed instead of raw first array item
- Goal: keep `F` positioned at a stable center reference in net visualization.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: manual exploration interaction
- Reworked page-2 flow from auto 7-cut playback to manual exploration.
- New behavior:
  - user clicks one target edge to cut one edge
  - each cut shows a visible slit on the original cube edge view
  - after >=3 cuts, user can select a face and move `Door Angle` slider to open that face like a door
- Added lock styling for non-target edges of current net.
- Preserved final net shape panel for reference.
- Kept all in-app text in English.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.
- Additional runtime logic check via node script:
  - all 11 demos validated as `6 faces / 5 hinges / 7 cuts`.

### Key decisions
- Preserve existing page-1 interaction and add page switch tabs instead of replacing flow.
- Keep cut-edge naming aligned with page-1 edge IDs (`前-上`, `后-左` etc.) for consistency.

## Update: unfold animation refinement
- Revised page-2 unfold animation from planar interpolation to hinge-based 3D unfolding.
- Implemented face-tree kinematics per selected net:
  - build face adjacency tree from net face positions
  - recursively rotate child faces around hinge axes from 90° (folded cube) to 0° (flat net)
- Updated animation button text to `从立方体展开`.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: step-by-step scissor workflow + English UI
- Changed page-2 animation to start from a closed cube view (not pre-unfolded).
- Added step sequence runner:
  - cuts exactly one edge per step (7 steps)
  - highlights active edge + scissor movement on cube
  - after each cut, face opening progresses in hinge-door style
- Added sequence reset action and per-step status text.
- Converted app UI strings to English in:
  - `src/main.tsx`
  - `src/game-spotlight.tsx`
  - `light-app.json`
  - `index.html`
  - `game-spotlight.html`

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: true 3D rotation + perspective projection (page 2)
- Replaced page-2 pseudo-3D CSS transform rendering with geometry-driven pipeline in `cube-surface-lab/src/main.tsx`.
- Added face-edge local vertex indexing and unified hinge-side index mapping for door rotation.
- Door-opening faces now rotate in 3D model space, then all visible faces are rendered through:
  - view rotation (`yaw` + `pitch`),
  - perspective projection,
  - depth sorting + backface culling.
- Edge rendering now projects 3D edge endpoints (including opened-door edges) and reuses this geometry for scissor motion path.
- Removed stale 2D face polygon dependency from page-2 cube rendering.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.

## Update: cube face completeness + label orientation rule
- Page-2 3D cube rendering no longer drops faces due aggressive backface culling; all cube faces are projected and depth-sorted for complete visualization.
- Adjusted fold orientation mapping (`east/west`) so net lettering follows requested orientation rule: center reference `F`, with `U` above and `R` on the right.

### Verification
- `cd light-maths/apps/cube-surface-lab && npm run build` passed.
- `cd light-maths && npm run build:all` passed.
