# 2026-03-17

## What changed
- Added new app `light-maths/apps/bias-variance-range/`.
- Implemented four shooter profiles covering all Bias + Variance combinations.
- Added 10-shot target interaction, shot distribution plotting, mean-impact marker, and spread summary.
- Added `game-spotlight` page for classroom framing.
- Registered the app in `light-math-hub/src/data/apps.json`.
- Granted local dev access in `light-math-hub/src/data/currentUser.ts`.

## Key decisions
- Kept the app on the existing Vite multi-entry contract with `index.html` and `game-spotlight.html`.
- Used a single-target shooting metaphor where every shooter aims at the bullseye, so bias is read from mean offset and variance from spread.
- Reset shot history when switching shooter type to keep each 10-shot distribution clean and comparable.

## Open issues
- No additional manual browser pass was run for `/run/bias-variance-range` and spotlight routing; only build verification was completed.

## Next actions
- If needed, run the hub and apps host locally and verify the launch route plus mobile interaction feel.
