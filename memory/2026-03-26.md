# 2026-03-26

## What changed
- Added new light app `speed-chase-lab` under `light-maths/apps/`.
- Implemented a starfield pursuit demo with parameter controls, auto-play, live table updates, and SVG function curves.
- Added spotlight page, Hub registry entry, and local dev permission.

## Key decisions
- Chose a single-stage pursuit animation plus table-and-graph linkage as the main teaching interaction.
- Implemented the graph with plain SVG instead of adding a chart dependency.
- Stopped the animation exactly at the catch-up moment when the chaser is faster; kept a separate fallback duration for no-catch scenarios.

## Open issues
- No known functional issues after build and browser verification.

## Next actions
- Deploy when ready via `./scripts/deploy-math-chat1.sh`.
