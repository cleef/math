## docs/mistakes_collection
- 变更：新增 `docs/mistakes_collection/20260406.md`，按 `20260310` 的题卡结构整理 5 道错题。
- 内容：补全了分糖、分巧克力、往返二次相遇、解方程、整数构成这 5 题的正确答案、解法、常见错误原因与易错提醒。
- 下一步：后续若继续补错题集，可沿用同一版式继续按日期新增。

## allocation-expression-lab
- 迭代：按 `SOUL.md` 中受 Caucher Birkar 启发的理念，重做 `allocation-expression-lab` 首页结构与视觉层次，改成更强的“ideas studio / 先观察再抽象”叙事。
- 变更：`src/App.tsx` 新增 hero、故事导入、观察 prompts、bridge strip、reflection cards，把主流程从“判断对错”转成“先看不变量、再辨别变化落在哪一层、最后观察两个表达式相遇”。
- 变更：`src/index.css` 重写为黑板 + 纸面 + 星图式视觉语言，强化探索感，同时保留现有播放、表达式和滑杆逻辑。
- 变更：`src/game-spotlight.tsx` 同步改写，对外说明更新为“数学是 ideas 的世界”“方程是两个表达式的相遇”。
- 决策：保留现有场景数据与表达式计算函数，不动题目 contract，只优化体验、语言和教学节奏，降低改坏题意的风险。
- 迭代：继续按“visual meaning over abstract intimidation”做减法，移除了场景 badge、阶段标题、bridge strip、reflection cards、关系卡顶部标签、收敛提示等解释性文字，让故事、动画、关系流和结果状态自己承担结构表达。
- 迭代：进一步去掉 `allocation-expression-lab` 的 hero 文案区，只保留纯视觉 hero，不再用标题、导语或 chips 解释页面。
- 迭代：把苹果装袋、巧克力分发里“剩余 / 多出”的数量区改成真实物品图标加数量显示，不再只显示数字胶囊；同时把场景标题 `分糖缺颗` 改成 `分糖`。
- 变更：更新根目录 `AGENTS.md`，新增 Frontend Design Guide，明确要求 `Reduce instructional text — trust the visual`，并强调 `visual meaning over abstract intimidation`、`the structure should communicate itself`。
- 验证：`cd light-maths/apps/allocation-expression-lab && npm run build`、`cd light-math-hub && npm run build`、`cd light-maths && npm run build:all` 均通过。
- 发布：执行 `cd /Users/lee/git/math && ./scripts/deploy-math-chat1.sh`，发布 release `20260406-223512` 到 `math.chat1.co`；首页和 `/apps/allocation-expression-lab/` 冒烟检查均返回 `HTTP 200`。
- 开放项：尚未做真实浏览器人工验收；若继续打磨，下一步应检查移动端可读性和按钮层级是否还可再压缩。

## articles
- 变更：新建 `articles/` 目录，新增公众号文章 `articles/2026-04-06-allocation-expression-lab-wechat.md`。
- 配图：通过 Playwright 从本地 `allocation-expression-lab` 页面截取 4 张图，放入 `articles/allocation-expression-lab/`。
- 内容：文章围绕“孩子把 `10x - 12` 写成 `10x + 12` 往往不是不会算，而是还没看见数量结构”展开，结合 app 的故事、二选一表达、关系流与滑杆相遇状态来写。
- 迭代：按新要求将文章改写为“两个思维误区”结构，重点讲解：
  - 把“需要”和“实际”混淆
  - 把多个关系（如床和人、袋和苹果）混淆
