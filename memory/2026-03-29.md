# 2026-03-29

- Changed hub app availability:
  - kept `cosmic-chase` enabled/listed and added local dev permission
  - marked `speed-chase-lab` as `enabled: false` and `listed: false` in Hub registry
- Changed hub list interaction:
  - app card click now launches `/run/:id` directly instead of opening detail first
  - detail page remains reachable from a hover/focus-only `课程介绍` / `Overview` button on each card
- Reduced Hub tag taxonomy to one tag per app in both `zh-CN` and `en-US` metadata.
- Verification:
  - `cd light-math-hub && npm run build`
  - `cd light-maths && npm run build:all`
- Open issues:
  - `light-maths build:all` still builds `speed-chase-lab` because it scans app folders rather than Hub visibility flags.
- Next actions:
  - If deployment should skip disabled apps during app-host build too, update `light-maths/scripts/build-all.mjs` to read Hub registry.
