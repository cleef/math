## meet-again-lab
- 变更：将 `meet-again-lab` 前置页「路程 = 速度 × 时间」四个路线演示中的“浪”字角色替换为马里奥像素风小狗精灵，新增 `src/components/PixelDogSprite.tsx`。
- 决策：保留原有路径和速度逻辑，仅替换角色渲染；按运动方向自动左右翻转小狗，避免影响原有教学演示节奏。
- 迭代：根据页面实际观感，重画为更明确的侧身四足小狗轮廓，并放大显示尺寸，强化耳朵、鼻口、腿和项圈的识别度。
- 迭代：在用户将页面内显示尺寸收敛到 `18` 后，小狗精灵进一步从像素块风格改为更简洁的 SVG 图标式侧身狗，减少细节，提升小尺寸下的可识别性。
- 变更：为 `meet-again-lab` 的“跑走势长直道折返 -> 跑直线并在跑道长度不够时自动延展”需求创建独立 worktree，路径为 `.worktrees/meet-again-lab-straight-track-scroll`，分支为 `feat/meet-again-lab-straight-track-scroll`。
- 决策：根目录 `.gitignore` 新增 `.worktrees/`，并以独立提交 `efcab8d` 固化，避免项目内 worktree 内容进入版本控制。
- 验证：新 worktree 内执行 `cd light-maths && npm run build:all` 通过，基线可直接用于后续实现“类似汽车前进、两旁树木后退”的滚动直道效果。
- 变更：已将 worktree 中的“跑直线持续前进 + 超长后横向滚动延展”逻辑合并回主分支，并保留主分支里的 `PixelDogSprite` 作为前置页角色。
- 决策：首个路线卡片恢复为和其它三张一致的简洁轨道风格，不再使用卷轴背景；行为差异仅保留为线性路线不折返、持续向前并自动滚动跟随。
- 迭代：首卡初始跑道长度已拉长到接近组件可视宽度，超过可视区后显示横向滚动并自动跟随角色位置。
- 验证：主工作区执行 `cd light-maths/apps/meet-again-lab && npm run build` 通过。
- 开放问题：暂无。
- 下一步：如需进一步强化视觉一致性，可继续把主题页中的人物圆点也替换为角色化图标。

## allocation-expression-lab
- 变更：新增实施方案文档 `docs/plans/2026-04-04-allocation-expression-lab.md`，规划一个“分配问题表达实验” app。
- 决策：首版聚焦“分配问题正确列式”，采用“确定未知数 -> 写两个表达式 -> 列方程检查”的三段式学习流程。
- 决策：首版交互优先采用受约束的表达式拼装，不先做自由文本解析，直接针对 `10x - 12` / `10x + 12` 这类符号方向错误。
- 内容：首批题型至少包含“房间分配”和“苹果分配”两个故事卡，并为每个条件配置常见错式与错因说明。
- 迭代：方案已升级为“双阶段教学”，先用具体数量做分配动画演示，支持开始、暂停、重置，再抽象成含未知数的表达式与方程。
- 迭代：方案新增场景按钮区，除内置“房间分配 / 苹果分配”外，增加“AI 生成新场景”，技术上走现有 `/api/apps/allocation-expression-lab/*` 网关。
- 决策：AI 场景必须产出与内置题同构的数据结构，且在本地未配置模型时要优雅降级，不能影响内置题可用性。
- 变更：已完成 `light-maths/apps/allocation-expression-lab/` 实现，包含场景切换、具体数量动画演示、暂停/重置、符号判断、试代检查和 `game-spotlight` 页面。
- 迭代：主页面已改为学生直操作界面，去掉“选择场景 / 第一部分 / 教学说明”等说明性文字，仅保留题目、按钮和必要状态。
- 迭代：`条件 1 / 条件 2` 的演示已改为上下堆叠，且每个条件各自拥有独立的开始、暂停、重置控制，不再共用一套播放条。
- 迭代：分配动画已从逐个小点填充改为按“每批 = 每组容量”展示，例如每间 `10` 名、每袋 `8` 个，以整批进入的批次条呈现。
- 变更：已接入 Hub 注册和本地权限，app id 为 `allocation-expression-lab`，路径为 `/apps/allocation-expression-lab/`。
- 验证：`cd light-math-hub && npm run build`、`cd light-maths && npm run build:all`、新 app 独立 `npm run build` 均通过；本地 API `/health` 和未配置 AI 时的降级提示也已验证。
- 开放问题：二期是否加入自由输入、更多应用题素材包，以及已配置 API Key 时的真实 AI 场景出题质量调优，待后续迭代。
- 下一步：如需继续，可以补 AI 场景生成在已配置 `OPENAI_API_KEY` 环境下的真机联调和手工 UI 走查。
