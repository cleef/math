# 2026-02-27

## What changed
- Performed release-readiness check for `light-maths/apps/density`.
- Verified build and deploy scripts against app packaging contract.

## Key decisions
- `density` is **not publishable** in current state.
- Must align with Light Math app contract before enabling release.

## Open issues
- Missing required `light-app.json`.
- Missing `game-spotlight.html` multi-entry page.
- `vite.config.ts` does not set `base=/apps/<id>/` or output `../../dist/apps/<id>`.
- App not registered in Hub `light-math-hub/src/data/apps.json` (enabled+listed gate).
- `build:all` skips this app because it filters by existence of `light-app.json`.

## Next actions
- Add/align manifest and Vite build config.
- Add spotlight entry page.
- Register app in Hub and permission mapping for local dev.
- Re-run `npm run build` (app), `npm run build:all` (apps), and hub smoke checks.

## Update
- Completed packaging fixes for `density`: added `light-app.json`, `game-spotlight.html`, and `src/game-spotlight.tsx`.
- Updated `density` Vite config to standard Light App output contract (`/apps/density/` base and `../../dist/apps/density` outDir with multi-entry input).
- Simplified `density/package.json` to frontend-only dependencies and generated `package-lock.json` after install.
- Integrated `density` into Hub registry and local permission map:
  - `light-math-hub/src/data/apps.json`
  - `light-math-hub/src/data/currentUser.ts`
- Verification passed:
  - `cd light-maths/apps/density && npm run build`
  - `cd light-maths && npm run build:all`
  - `cd light-math-hub && npm run build`

## Remaining concerns
- `npm install` in `density` reports 2 moderate vulnerabilities (non-blocking for current release; pending dependency security follow-up).
